<%= form_for(@goody, :html => { :multipart => true }) do |f| %>
  <% if @goody.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goody.errors.count, "error") %> prohibited this goody from being saved:</h2>

      <ul>
      <% @goody.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>
  <div class="field">
    <%= f.file_field :goody_file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
